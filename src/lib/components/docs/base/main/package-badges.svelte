<script lang="ts">
	import { Badge } from "$lib/components/ui/spell/badge";
	import { cn } from "$lib/utils";

	type PackageBadgesProps = {
		packages?: string[];
		class?: string;
	};

	let { packages = [], class: className }: PackageBadgesProps = $props();
</script>

{#if packages.length > 0}
	<div
		class={cn("mt-4 flex flex-wrap gap-2", className)}
		data-toc-ignore="true"
	>
		{#each packages as pkg (pkg)}
			<Badge
				variant="secondary"
				class="rounded-full border-border/60 bg-muted/60 px-2 py-1 font-mono text-xs leading-none text-muted-foreground"
			>
				{pkg}
			</Badge>
		{/each}
	</div>
{/if}
